package main

import (
	"log"
	"os"
	"os/signal"
	"syscall"
)

func main() {

	TCPServer()
	log.Println("Server Started")

	c := make(chan os.Signal)
	signal.Notify(c, os.Interrupt, syscall.SIGTERM)
	<-c
}
